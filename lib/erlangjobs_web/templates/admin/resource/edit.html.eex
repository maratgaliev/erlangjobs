<h2 class='text-muted'>РЕДАКТИРОВАНИЕ РЕСУРСА</h2>

<%= render "form.html", changeset: @changeset,
                        action: admin_resource_path(@conn, :update, @resource) %>

<span><%= link "Назад", to: resource_path(@conn, :index) %></span>
