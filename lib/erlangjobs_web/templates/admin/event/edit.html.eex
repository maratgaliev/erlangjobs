<h2 class='text-muted'>РЕДАКТИРОВАНИЕ СОБЫТИЯ</h2>

<%= render "form.html", changeset: @changeset,
                        action: admin_event_path(@conn, :update, @event) %>

<span><%= link "Назад", to: event_path(@conn, :index) %></span>
